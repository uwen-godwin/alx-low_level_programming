#include <stdio.h>

/**
 * File: 101-natural
 * Auth: Godwin uwenn
 */
int main(void)
{
	int, sum = 0;
	for (i = 0; i < 1024; i++)
	{
		if ((i % 3) == || (i % 5 ) == 0)
	}
	printf("%d", sum)
		return (0)
}
