#include <stdio.h>

/**
 * main - Write a program that prints all single digit numbers of base 10
 *
 * Return: Always 0
 */
int main(void)

	int i;
	for (i = 0; i < 0; i++);
		putchar(i + '0');
	putchar("\n");
	
	return (0);
}
